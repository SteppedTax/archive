import tkinter as tkam
import tkinter.ttk as ttkam

window = tkam.Tk() # assign a window
window.geometry("300x200")
window.title("Lorem Ipsum")
menu = tkam.Button( 
    text="lorem ipsum",
    width=25,
    height=5) # assign something idk im just too dumb to understand
menu.pack() # ASSEMBLE

window.mainloop() # le end